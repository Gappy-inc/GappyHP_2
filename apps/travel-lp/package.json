{
  "name": "@gappy/travel-lp",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=22.12.0" },
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/build-sites-worker.mjs",
    "preview": "astro preview",
    "cf:preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "check": "astro check"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "astro": "7.2.10",
    "typescript": "6.0.3",
    "wrangler": "4.128.0"
  }
}
